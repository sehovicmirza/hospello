# Read for everyone on the staff, write for hotel admins only — the same
# split WhatsappChannelPolicy and friends encode, and for the same reason on
# each side.
#
# A receptionist reads this to answer "why did that message not go out?" —
# a template still pending at Meta is the most likely answer, and it is a
# shift question. Recording what Meta decided is not: it is bookkeeping about
# the hotel's own registration, and a wrong entry sends someone hunting for a
# problem in the wrong place.
class WhatsappTemplatePolicy < HotelConfigurationPolicy
end
