<% content_for :title, t(".title", entry_title: @entry.title) %>

<%= link_to t(".back"), staff_kb_entries_path,
      class: "text-sm font-medium text-blue-600 hover:text-blue-500" %>

<h1 class="mt-3 mb-6 text-2xl font-semibold text-gray-900"><%= t(".heading", entry_title: @entry.title) %></h1>

<div class="max-w-2xl">
  <%= render "staff/kb_entries/form", entry: @entry, url: staff_kb_entry_path(@entry) %>
</div>
