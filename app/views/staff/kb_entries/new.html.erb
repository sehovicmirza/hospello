<% content_for :title, "New knowledge base entry — #{Current.hotel.name}" %>

<%= link_to "← Back to the knowledge base", staff_kb_entries_path,
      class: "text-sm font-medium text-blue-600 hover:text-blue-500" %>

<h1 class="mt-3 mb-6 text-2xl font-semibold text-gray-900">Add an entry</h1>

<div class="max-w-2xl">
  <%= render "staff/kb_entries/form", entry: @entry, url: staff_kb_entries_path %>
</div>
