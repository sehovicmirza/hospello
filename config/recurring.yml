# examples:
#   periodic_cleanup:
#     class: CleanSoftDeletedRecordsJob
#     queue: background
#     args: [ 1000, { batch_size: 500 } ]
#     schedule: every hour
#   periodic_cleanup_with_command:
#     command: "SoftDeletedRecord.due.delete_all"
#     priority: 2
#     schedule: at 5am every day

production:
  clear_solid_queue_finished_jobs:
    command: "SolidQueue::Job.clear_finished_in_batches(sleep_between_batches: 0.3)"
    schedule: every hour at minute 12

  # Ops::HeartbeatJob runs from the queue, not from a web request, precisely
  # so a dead job pipeline behind a healthy web process stops pinging and the
  # external monitor pages someone — see the job for why.
  ops_heartbeat:
    class: Ops::HeartbeatJob
    queue: low
    schedule: every 5 minutes

  ops_queue_health:
    class: Ops::QueueHealthJob
    queue: low
    schedule: every 10 minutes

  # A guest who abandons a half-finished request mid-conversation must not
  # have it spring to life an hour later — and the draft they left behind
  # holds the one-live-draft slot until it is closed. Every 5 minutes rather
  # than hourly because the window that matters is the guest's own visit.
  expire_service_request_drafts:
    class: ServiceRequests::ExpireDraftsJob
    queue: low
    schedule: every 5 minutes

  # A message left mid-translation because its job died shows the reader
  # "translating…" forever, which is worse than showing them the original.
  # Every minute rather than every five: the budget it enforces is fifteen
  # seconds, and a sweep an order of magnitude slower than its own budget is
  # not really enforcing one.
  settle_overdue_translations:
    class: Ai::TranslationWatchdogJob
    queue: low
    schedule: every minute

  # What makes docs/plan/engineering-rules.md's promise to guests true, and
  # app/services/retention/policy.rb the thing that decides it. Once a day,
  # not hourly: every window it enforces is measured in months, so running it
  # more often would only mean doing the same scan 23 more times to find
  # nothing. 03:20 because the queue is otherwise idle then and this is the
  # largest delete this app ever does — and at :20 rather than on the hour so
  # it does not start alongside the hourly Solid Queue cleanup above.
  #
  # A run that is missed entirely (a deploy, an outage) costs nothing: the
  # next one purges everything the missed one would have, because the cutoffs
  # are computed from the moment it runs rather than from the last run.
  purge_expired_guest_data:
    class: Retention::PurgeExpiredGuestDataJob
    queue: low
    schedule: every day at 3:20am

  # Later slices add more recurring entries here: escalation reminders.
